# .env.test
# Kredensial dummy untuk MySQL testing container (docker-compose.test.yml)
# Data di container ini ephemeral (tmpfs) — aman untuk di-commit, JANGAN gunakan
# kredensial seperti ini untuk environment production/staging.

DATABASE_URL=mysql+aiomysql://testuser:testpass@localhost:3307/pyapi_test
